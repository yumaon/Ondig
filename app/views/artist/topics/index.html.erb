<div class="container">
  <div class="row">
    <div class="col-md-12 text-white">
      <h3>Topics</h3>
    </div>
    <% if artist_user_signed_in? %>
    <div class="col-md-12 text-white">
      <%= link_to "Topicを投稿する", new_artist_topic_path %>
    </div>
    <% end %>

    <div class="col-md-12">
      <% @topics.each do |topic| %>
        <div class="topic-box">
          <%= link_to artist_topic_path(topic) do %>
            <div class="d-flex mt-3 p-3">
              <div class="">
                <%= image_tag topic.artist_user.profile_image, class: "mini-profile-image" %>
              </div>
              <div class="ml-4 d-flex align-items-center flex-fill">
                <%= simple_format topic.body, class: "text-white" %>
              </div>
            </div>
            <% if topic.artist_user_id == current_artist_user.id %>
              <div class="flex-fill text-right mr-4 pb-2">
                <%= link_to "編集", edit_artist_topic_path(topic), class: "btn btn-follow btn-success mr-3" %>
                <%= link_to "削除", artist_topic_path(topic), method: :delete, class: "btn btn-follow btn-success" %>
              </div>
            <% end %>
          <% end %>
        </div>
      <% end %>
    </div>
  </div>
</div>